# Team photos

Drop each member's profile picture in this folder (jpg/png/webp, square images look best).

Then reference it in `src/data/team.ts` as:

```ts
photoUrl: '/team/yourfile.jpg'
```

Square-crop ~400x400 is plenty; the UI renders them as circles.
